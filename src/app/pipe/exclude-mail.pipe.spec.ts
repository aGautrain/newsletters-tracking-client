import { ExcludeMailPipe } from './exclude-mail.pipe';

describe('ExcludeMailPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcludeMailPipe();
    expect(pipe).toBeTruthy();
  });
});
